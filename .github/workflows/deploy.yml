name: Deploy
on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-prod
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION:   ap-southeast-2                                          # ← from discovery
  ECR_REPO:     tiny-shortener
  ECS_CLUSTER:  default                                         # ← from discovery
  ECS_SERVICE:  tiny-shortener                                 # ← from discovery
  TASK_FAMILY:  default-tiny-shortener                                         # ← from discovery
  CONTAINER:    Main                                                     # ← from discovery
  PUBLIC_URL:   ti-71ead734d9854275bf7d545a932843d8.ecs.ap-southeast-2.on.aws  # ← from discovery

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::460589075034:role/GhDeployTinyShortener   # ← your account ID
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2
        id: ecr

      - name: Set up buildx (multi-platform builds)
        uses: docker/setup-buildx-action@v3

      - name: Build, tag, push (force linux/amd64 for Fargate)
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
        run: |
          SHA=${GITHUB_SHA::7}
          IMG=$REGISTRY/$ECR_REPO:$SHA
          docker buildx build --platform=linux/amd64 -t $IMG --push .
          echo "IMG=$IMG" >> $GITHUB_ENV

      - name: Register new task definition revision with the new image
        id: taskdef
        run: |
          CURRENT=$(aws ecs describe-task-definition --task-definition $TASK_FAMILY \
                    --query 'taskDefinition' --output json)
          NEW=$(echo "$CURRENT" | jq --arg img "$IMG" --arg c "$CONTAINER" \
                  '.containerDefinitions |= map(if .name == $c then .image=$img else . end)' | \
                jq 'del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
          REVISION=$(aws ecs register-task-definition --cli-input-json "$NEW" \
                    --query 'taskDefinition.taskDefinitionArn' --output text)
          echo "REVISION=$REVISION" >> $GITHUB_OUTPUT

      - name: Roll out to ECS (force a new deployment with the new revision)
        run: |
          aws ecs update-service \
            --cluster $ECS_CLUSTER --service $ECS_SERVICE \
            --task-definition ${{ steps.taskdef.outputs.REVISION }} \
            --force-new-deployment
          aws ecs wait services-stable \
            --cluster $ECS_CLUSTER --services $ECS_SERVICE

      - name: Smoke test
        run: curl --fail --retry 10 --retry-delay 15 https://$PUBLIC_URL/health

